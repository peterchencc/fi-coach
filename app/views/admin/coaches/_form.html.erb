<%= form_for(@admin_coach) do |f| %>
  <% if @admin_coach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_coach.errors.count, "error") %> prohibited this admin_coach from being saved:</h2>

      <ul>
      <% @admin_coach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coach_name %><br>
    <%= f.text_field :coach_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
