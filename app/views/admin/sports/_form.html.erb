<%= form_for(@admin_sport) do |f| %>
  <% if @admin_sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_sport.errors.count, "error") %> prohibited this admin_sport from being saved:</h2>

      <ul>
      <% @admin_sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
